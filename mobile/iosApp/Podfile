platform :ios, '16.0'
use_frameworks!
use_modular_headers!

target 'iosApp' do
  # KMP shared module — podspec generated by Gradle's :composeApp:podspec task.
  # Run `./gradlew :composeApp:podspec` before `pod install`.
  pod 'composeApp', :path => '../composeApp'

  pod 'FirebaseAnalytics', '~> 11.0'
  pod 'FirebaseCrashlytics', '~> 11.0'
end
